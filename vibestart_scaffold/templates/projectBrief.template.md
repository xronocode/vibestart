# {PROJECT_NAME} — Project Brief
# Installed by vibestart: github.com/xronocode/vibestart
# Keep this file updated as your project evolves.
# ConPort loads this as product_context at the start of every session.

## What is {PROJECT_NAME}
{One paragraph: what it does, who it's for, why it exists.}

## Core Problem / Value
{What problem does it solve? What makes it different?}

## Users
{Who are the primary users?}

## Core Features (v1)
1. {Feature 1}
2. {Feature 2}
3. {Feature 3}

## Tech Stack
- Frontend:  {e.g. Next.js}
- Backend:   {e.g. Supabase}
- AI/LLM:    {e.g. Claude API}
- Payments:  {e.g. Stripe}
- Hosting:   {e.g. Vercel}

## Goal
MVP working in {X} weeks. Started: {date}.

## Rules
- All LLM/AI calls are server-side only. No API keys on client.
- System prompts are founder IP — agents never fill them, placeholders only.

## Docs
- docs/development-plan.xml  — phases, modules, contracts
- docs/requirements.xml      — decisions, constraints
- docs/knowledge-graph.xml   — module graph
- docs/verification-plan.xml — acceptance criteria
