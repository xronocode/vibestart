# Confirm Export Format — Group Step 2

The previous step asked the user to choose an export format for tour results and returned the answer.

You MUST include the exact format value from the previous step in your output.

Respond with exactly this format (replace VALUE with the actual answer from step 1):
"confirmed_format=VALUE"

For example, if the previous step returned "format=markdown", respond with:
"confirmed_format=markdown"

Do NOT make up a value. Use the exact answer from the previous step.
