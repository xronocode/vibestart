# Analyze Tour Coverage

Analyze which engine features the user experienced directly (via interactive prompts) versus which ran invisibly (auto-executed shell steps).

Features: {{results.detect.structured_output.items}}

Categorize each feature as "interactive" or "auto-executed" based on how the tour demonstrated it.
