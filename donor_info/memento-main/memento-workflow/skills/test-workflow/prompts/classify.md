# Classify Engine Features

Classify each engine feature by learning curve: "basic" (easy to understand) or "advanced" (requires deeper knowledge).

Features: {{results.detect.structured_output.items}}

Return a plain text summary like: "shell=basic, conditional=advanced, retry=advanced"
