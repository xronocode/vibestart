# Ask User — Single Step

You MUST call the ask_user tool now. Do NOT skip this step.

Ask exactly this question:
Message: "How much detail in the tour summary?"
Options: ["brief", "detailed"]

After receiving the answer, respond with exactly:
"verbosity=<ANSWER>"

If you don't call ask_user, the test fails.
