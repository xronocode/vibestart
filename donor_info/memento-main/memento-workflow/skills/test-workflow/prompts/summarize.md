# Summarize Tour Results

Summarize the engine capabilities tour as structured JSON.

Features covered: {{results.detect.structured_output.count}}
Tour depth: {{results.mode.output}}

Return a JSON object with:
- total_items: number of features covered
- status: "complete"
- notes: a brief summary of which engine capabilities were demonstrated
